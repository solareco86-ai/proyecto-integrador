# Dependencias de desarrollo para auditoría UI/UX automatizada
# Instalación:
#   source venv/bin/activate
#   pip install -r requirements-dev.txt
#   playwright install chromium

playwright>=1.40.0

# Testing (movidas desde requirements.txt)
pytest>=8.4.2
pytest-asyncio>=0.26.0
pytest-cov>=5.0.0
coverage>=7.14.1
httpx>=0.28.1

# SQLite async para desarrollo local (MySQL + aiomysql en prod)
aiosqlite>=0.20.0

# Linting
ruff>=0.8.0

# Verificación estática de tipos (modo strict)
pyright>=1.1.380
